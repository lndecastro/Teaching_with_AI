name: Deploy Jupyter Book to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Clean previous Book builds
        run: jupyter-book clean TaLwAI --all || true

      - name: Build Jupyter Book
        run: jupyter-book build TaLwAI

      - name: Deploy to GitHub Pages
        run: ghp-import -n -p -f TaLwAI/_build/html
